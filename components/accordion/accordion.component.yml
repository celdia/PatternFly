name: Accordion
description: "An interactive container that expands and collapses to hide or reveal nested content."
links:
  - "https://www.patternfly.org/components/accordion"
variants:
  default:
    title: Default
  bordered:
    title: Bordered
slots:
  items:
    title: Items
stories:
  default:
    title: Default
    slots:
      items:
        - type: component
          component: "patternfly:accordion_item"
          story: default
        - type: component
          component: "patternfly:accordion_item"
          story: default
