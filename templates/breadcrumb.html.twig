{#
/**
 * @file
 * Default theme implementation for a breadcrumb trail.
 *
 * Available variables:
 * - breadcrumb: Breadcrumb trail items.
 *
 * @ingroup themeable
 */
#}
{% if breadcrumb %}
  {{ include('patternfly:breadcrumb', {
    items: breadcrumb,
  }, with_context=false) }}
{% endif %}
